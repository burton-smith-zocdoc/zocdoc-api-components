{
  // Solution file — it owns no sources of its own. Each package emits into its own `dist`,
  // which is what the packages' `exports` maps point at and where `build-theme.ts` already
  // writes the theme CSS. A single root `dist` cannot satisfy both.
  "files": [],
  "references": [
    { "path": "packages/primitives/tsconfig.build.json" },
    { "path": "packages/api-components/tsconfig.build.json" }
  ]
}
