{
  "name": "powered-by-zocdoc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm run build:types && pnpm run build:theme && pnpm run cem && pnpm -r run cem",
    "build:types": "tsc --build tsconfig.build.json",
    "build:theme": "oxnode scripts/build-theme.ts",
    "typecheck": "pnpm run build:types && tsc --noEmit",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "test": "pnpm run build:types && vitest run",
    "test:client": "pnpm run build:types && vitest run --project client",
    "test:components": "pnpm run build:types && vitest run --project components",
    "cem": "cem generate && pnpm -r run cem",
    "docs:check": "pnpm run cem && pnpm -r run cem && (DRIFT=\"$(git status --porcelain -- .claude/skills/*/references packages/*/custom-elements.json)\"; if [ -n \"$DRIFT\" ]; then echo 'Generated docs are stale — regenerate and commit the changes below:' >&2; echo \"$DRIFT\" >&2; exit 1; fi)",
    "storybook": "pnpm run build:types && pnpm run cem && storybook dev -p 6006",
    "storybook:build": "pnpm run build:types && pnpm run cem && storybook build",
    "demo": "pnpm --filter demo dev"
  },
  "devDependencies": {
    "@charm-ux/theming": "link:../charm-ux/core/packages/theming",
    "@oxc-node/cli": "^0.1.0",
    "@storybook/addon-docs": "^10.5.6",
    "@storybook/web-components": "^10.5.5",
    "@storybook/web-components-vite": "^10.5.5",
    "@types/node": "^24.3.0",
    "@vitest/browser": "^4.1.10",
    "@vitest/browser-playwright": "^4.1.10",
    "@wc-toolkit/cem-generator": "^0.1.8",
    "@wc-toolkit/cem-generator-cli": "^0.1.9",
    "@wc-toolkit/cem-generator-lit": "^0.1.9",
    "@wc-toolkit/storybook-helpers": "^10.7.3",
    "axe-core": "^4.12.1",
    "lit": "^3.2.1",
    "oxfmt": "^0.62.0",
    "oxlint": "^1.77.0",
    "playwright": "^1.62.1",
    "storybook": "^10.5.5",
    "typescript": "^5.9.3",
    "vite": "^7.1.5",
    "vitest": "^4.1.10"
  },
  "customElements": "custom-elements.json"
}
